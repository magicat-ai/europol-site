---
interface Props {
  title?: string;
  text?: string;
}

const { 
  title = "Hai Bisogno di un'Investigazione?",
  text = "Contatta EUROPOL® per una consulenza investigativa gratuita e completamente riservata. Analizzeremo insieme la tua situazione e ti forniremo un preventivo dettagliato senza alcun vincolo."
} = Astro.props;
---

<div class="bg-gradient-to-br from-gray-50 to-white border-t border-b py-12 my-16" style="border-color: rgba(11, 11, 11, 0.1);">
  <div class="container mx-auto px-4 max-w-4xl">
    <div class="text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-4" style="color: var(--color-black); font-family: 'Playfair Display', serif;">
        {title}
      </h2>
      <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
        {text}
      </p>
      <a 
        href="/contatti/"
        class="inline-block px-8 py-4 rounded font-semibold text-lg transition-all duration-200 hover:scale-105"
        style="background-color: var(--color-black); color: white;"
      >
        Richiedi Consulenza Gratuita
      </a>
    </div>
  </div>
</div>

